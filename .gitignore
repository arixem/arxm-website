# Variables de entorno locales para `wrangler pages dev` (contiene NOTIFICATIONS_KEY).
# La key real vive como secreto encriptado en Cloudflare Pages, nunca en git.
.dev.vars
.dev.vars.*

# Cloudflare Pages / Wrangler
.wrangler/

# Instrucciones locales para Claude Code (no se versiona)
CLAUDE.md

# Bitacoras de sesion (notas internas de trabajo, no se versionan)
docs/_vault/01-sesiones/
